using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    //void Start()
    //{
    //    AdjustCameraToScreenSize();
    //}

    //void AdjustCameraToScreenSize()
    //{
    //    Camera mainCamera = Camera.main;

    //    if (mainCamera != null)
    //    {
    //        float targetAspectRatio = 16f / 9f; // Varsayýlan hedef aspect ratio (örn. 16:9)
    //        float currentAspectRatio = (float)Screen.width / Screen.height;

    //        // Ekran boyutu deðiþikliklerini dikkate alarak kamera açýsýný otomatik olarak ayarlayýn.
    //        mainCamera.fieldOfView = Mathf.Lerp(mainCamera.fieldOfView, mainCamera.fieldOfView * (targetAspectRatio / currentAspectRatio), Time.deltaTime);
    //    }
    //}
}
